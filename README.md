[![Build on Linux](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml)
[![Linux musl static +(Cross-Platform)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml)

# 🌀 FreshVRAM (ex Balidan): Universal Terminal Monolith
> **Пишется и ViewPort + MouseX10(а другие и не надо) уже работают**

**Автор:** Алексей Васильевич Поздняков ([avp70ru@mail.ru](mailto:avp70ru@mail.ru))  
**Лицензия:** GPLv3  
**Дата создания:** Февраль 2026

---

## 🌍 Глобальная концепция (Законы Мира)

Архитектура проекта — это переосмысленный терминал, работающий напрямую с физической памятью через системные вызовы. Весь мир представлен как единый монолитный буфер, разделенный на функциональные слои.

1. **Пространство (`GlobalBuf`)**: Единая непрерывная память (~389 Мб для 8192*5062 utf8) легко организовать теневые экраны при этом не забываем про масштаб шрифта, по сути мышью растянули или шрифт уменьшили, всё работает).
   - **Смещение**: Системный сектор. Настройка разрядности через `typedef uintptr_t Cell`.
   - **Мультиразрядность**: Все указатели и смещения адаптируются под 16/32/64/128/...-битные системы (Cell-based).

2. **Матрица (Planar VRAM)**:
   Разделение данных на независимые плоскости (слои) для максимального Cache-hit:
   - **G_DATA**: UTF-8 поток, 32 Кб на строку. Защита от цепочек UTF8 (CJK, Emoji, Zalgo (cp >= 0x1F300)).
   - **G_ATTRIBUTE**: Сверхбыстрая лента атрибутов (Цвет + Жирность + Инверсия) для токена целиком.
   - **G_TOKENS**: Атомарная структура мира. Для каждого токена хранится длина в байтах и визуальная ширина.
   - **G_LINE**: Компактные метаданные смещений токенов в строках.

3. **Световод (Engine)**: 
   Высокоскоростной рендерер, использующий **SWAR (SIMD Within A Register)**:
   - Сканирование атрибутов идет разом в разрядность процессора (для 64 по 8 токенов разом). 
   - Маска **`0x81818181...`** позволяет за один такт проверить (для 64 по 8 токенов разом) на наличие изменений.
   - При обнаружении изменения происходит вывод блока в терминал разом.

4. **Синхронизация и Графика**:
   - **Лупа (Viewport)**: Мгновенный прокрутка по миру простым смещением реального курсора.
   - **Брайль-Графика**: Использование символов Брайля (U+2800) превращает текстовую сетку в битмап-дисплей высокого разрешения.

---

## 🛠 Технологии и Оптимизация

- **Memory Management**: Прямые системные вызовы. Выравнивание по границам страниц (4 Кб).
- **Zero-Latency**: Неблокирующий ввод, отсутствие внешних библиотек и адаптивные задержки (чистый C11), игровой движок.
- **Адресная арифметика**: Вычисление всех смещений через битовые сдвиги.
- **Unicode Resilience**: Однозначное декодирование UTF-8 и учет визуальной ширины (CJK/Emoji) на уровне "токенов-атомов".

---
*Created in 2026. Non-blocking UI for maximum efficiency.*
## 🛠 Build & Tech / Сборка и Оптимизация
Проект кроссплатформенный (Linux/Windows/MacOs).

*   **Extreme Optimization**: Бинарный файл при статический линковке musl static занимает 37 кило байт.
*   **Static Analysis**: Чистый C11.

```bash
make         # Флаги Linux/Windows (' ' c g ) MacOs (mac) Linux static (musl g-musl)
./fresh      # Запуск
fresh.exe    #

*Created in 2026. Designed for speed, logic, and clarity.*
